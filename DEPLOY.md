# 🚀 Развертывание на Vercel

## ⚠️ Важно

Vercel лучше работает с **фронтенд-приложениями** (Next.js, React), а не с Flask. Есть **3 варианта**:

### Вариант 1: Vercel + Supabase (Рекомендуется)

- Простой, работает из коробки
- Vercel развертывает приложение
- Supabase работает как БД
- **Бесплатно до определённого лимита**

### Вариант 2: Railway (Проще всего)

- Полностью совместим с Flask
- Просто подключи GitHub
- 1 команда запуска
- **Бесплатный кредит $5 в день**

### Вариант 3: Render.com (Альтернатива)

- Похоже на Railway
- Поддерживает Flask
- Простое развертывание
- **Бесплатный уровень**

---

## ✅ Вариант 1: Vercel + Supabase (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создать БД на Supabase

1. Перейди на [https://supabase.com](https://supabase.com)
2. Нажми "Start your project" (или отпишись)
3. Создай новый проект (выбери регион)
4. Скопируй Connection String:
   - В боковом меню перейди Settings → Database → Connection strings
   - Скопируй строку для Python (psycopg2)

### Шаг 2: Подготовить приложение

Файлы уже готовы:

- ✅ `vercel.json` - конфиг для Vercel
- ✅ `config.py` - поддержка PostgreSQL
- ✅ `requirements.txt` - все нужные пакеты

### Шаг 3: Развернуть на GitHub

```bash
# Инициализировать Git репо
git init
git add .
git commit -m "Initial commit"

# Подключить удалённый репо (замени USERNAME и REPO)
git remote add origin https://github.com/USERNAME/REPO.git
git branch -M main
git push -u origin main
```

### Шаг 4: Подключить к Vercel

1. Перейди на [https://vercel.com/new](https://vercel.com/new)
2. Выбери "Import Git Repository"
3. Вставь URL твоего GitHub репо
4. Нажми "Import"
5. В настройках проекта:
   - Перейди Settings → Environment Variables
   - Добавь переменные:

```text
DATABASE_URL = (строка из Supabase)
SECRET_KEY = (любой сложный текст, например: your-super-secret-key-12345)
FLASK_ENV = production
```

### Шаг 5: Развернуть

1. Нажми "Deploy"
2. Дождись завершения
3. Открой URL приложения (vercel.com/...)

### Шаг 6: Инициализировать БД

После успешного развертывания БД пустая. Нужно создать таблицы:

#### Вариант А

Через Python скрипт:

```python
# Выполни локально:
from app import create_app, db
app = create_app('production')
with app.app_context():
    db.create_all()
```

#### Вариант Б

Через Vercel CLI:

```bash
npm install -g vercel
vercel env pull
python init_db.py
```

---

## ✅ Вариант 2: Railway (EASIEST!)

### Шаг 1: Подготовка

1. Создай GitHub репо и загрузи код:

```bash
git init
git add .
git commit -m "Initial"
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```

### Шаг 2: Развернуть на Railway

1. Перейди на [https://railway.app](https://railway.app)
2. Нажми "New Project" → "Deploy from GitHub"
3. Выбери свой репо
4. Railway автоматически:
   - ✅ Найдёт requirements.txt
   - ✅ Установит зависимости
   - ✅ Создаст Postgres БД
   - ✅ Назначит переменные окружения

### Шаг 3: Настроить переменные

В Railroad IDE:

- Добавь: `DATABASE_URL` (Railway создаст сама)
- Добавь: `SECRET_KEY = your-secret-key-123`
- Добавь: `FLASK_ENV = production`

### Шаг 4: Запустить

```bash
python init_db.py  # Инициализация
python run.py      # Запуск
```

**Адрес:** Railway выдаст тебе ссылку в консоли

---

## ✅ Вариант 3: Render.com

### Быстрый старт

1. [https://render.com](https://render.com) → Sign up
2. "New +" → "Web Service"
3. Connect GitHub репо
4. Настройки:
   - Build command: `pip install -r requirements.txt`
   - Start command: `gunicorn app:app`
5. Add env var:
   - `DATABASE_URL` - скопируй из Render
   - `SECRET_KEY` - любой ключ
6. Deploy

---

## 🆘 Если что-то сломалось

### Ошибка: "ModuleNotFoundError"

```bash
# requirements.txt нужны все пакеты
pip install -r requirements.txt --upgrade
```

### Ошибка: "DatabaseError"

БД пустая, нужно инициализировать:

```python
from app import create_app, db
app = create_app('production')
with app.app_context():
    db.create_all()
```

### Ошибка: "Port already in use"

Измени в run.py:

```python
app.run(debug=True, port=5001)  # Измени с 5000 на 5001
```

### Логи ошибок

- Vercel: Dashboard → Deployments → Logs
- Railway: IDE Console
- Render: Logs tab

---

## 📱 Проверка после развертывания

Откройте в браузере адрес приложения и проверьте:

- ✅ Главная страница загружается
- ✅ Можно зарегистрироваться
- ✅ Можно войти (admin/admin123)
- ✅ Можно создать пост
- ✅ Можно оставить комментарий

---

## 💡 Совет

**Для быстрого старта используй Railway!** Это самый простой и быстрый способ.

Для максимальной гибкости используй **Vercel + Supabase**.

---

## 🔗 Полезные ссылки

- Vercel: [https://vercel.com](https://vercel.com)
- Supabase: [https://supabase.com](https://supabase.com)
- Railway: [https://railway.app](https://railway.app)
- Render: [https://render.com](https://render.com)
