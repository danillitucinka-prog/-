# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО ЗАПУСКУ

## Шаг за шагом

### 1️⃣ Откройте PowerShell и перейдите в папку проекта

```powershell
cd "C:\Users\слава\Documents\GitHub\пивоо"
```

### 2️⃣ Установите зависимости

```powershell
pip install -r requirements.txt
```

⏳ Это займет 1-2 минуты. Будут установлены:

- Flask - веб-фреймворк
- SQLAlchemy - база данных
- Flask-Login - управление сессиями
- WTForms - валидация форм
- Werkzeug - безопасность

### 3️⃣ Создайте тестовые данные (опционально)

```powershell
python init_db.py
```

✅ Создаст:

- Администратора (admin/admin123)
- 5 тестовых пользователей
- 5 сообществ (сабреддитов)
- 8 тестовых постов
- Комментарии к постам

### 4️⃣ Запустите приложение

```powershell
python run.py
```

🎉 Готово! Откройте браузер и перейдите на: [http://localhost:5000](http://localhost:5000)

---

## 🔑 Учетные данные для входа

### Администратор (все права)

```text
Ник: admin
Пароль: admin123
```

### Тестовые пользователи

```text
john / password123
jane / password123
bob / password123
alice / password123
charlie / password123
```

---

## 📱 Основные действия

### Первый раз?

1. **Регистрация**
   - Кликните "Регистрация"
   - Введите ник (3-80 символов)
   - Введите email
   - Придумайте пароль (минимум 6 символов)
   - Нажмите "Зарегистрироваться"

2. **Вход**
   - На странице логина введите ник и пароль
   - Поставьте "Помнить меня" (опционально)
   - Нажмите "Войти"

3. **Создание сообщества**
   - В меню выберите "Создать сообщество"
   - Введите название (r/название)
   - Добавьте описание
   - Добавьте правила (опционально)
   - Нажмите "Создать"

4. **Создание поста**
   - Нажмите "Создать пост"
   - Выберите сообщество
   - Введите заголовок
   - Выберите тип (текст, ссылка, изображение)
   - Добавьте содержание
   - Нажмите "Опубликовать"

5. **Комментирование**
   - Откройте пост
   - Прокрутите вниз
   - Введите комментарий в форму
   - Нажмите "Комментировать"

---

## 🌐 Основные функции

### На главной странице

- **Сортировка**: Hot (горячее), New (новое), Top (топ)
- **Лайки/дизлайки**: ▲ и ▼ кнопки
- **Просмотр поста**: Нажмите на заголовок
- **Сохранение**: Нажмите "🔖 Сохранить"

### На профиле

- **Редактировать профиль**: История, статис, настройки
- **Смена пароля**: Отдельная страница
- **Мой аккаунт**: Все ваши посты и лайки
- **Подписки**: Список людей, на которых вы подписаны

### В сообществах

- **Присоединиться**: Нажмите кнопку "Присоединиться"
- **Выход**: Нажмите "Выход"
- **Просмотр постов**: Фильтр по сообществу

### Для админов (если вы admin)

- **Админ-панель**: Ссылка в меню
- **Блокировка пользователей**: Управление юзерами
- **Просмотр отчетов**: Модерация контента
- **Статистика**: Общая информация о сайте

---

## ⚙️ Расширенные настройки

### Изменение конфигурации

Откройте файл `config.py` и измените:

```python
# Количество постов на странице
POSTS_PER_PAGE = 20

# Таймаут между постами (секунды)
POST_COOLDOWN_SECONDS = 60

# Язык по умолчанию
DEFAULT_LANGUAGE = 'ru'  # или 'en'
```

### Изменение порта

```powershell
# Отредактируйте run.py:
# Измените: app.run(port=5000)
# На:       app.run(port=8000)
```

### Использование другой БД

В `config.py`:

```python
# SQLite (по умолчанию)
SQLALCHEMY_DATABASE_URI = 'sqlite:///app.db'

# PostgreSQL (требует установки)
SQLALCHEMY_DATABASE_URI = 'postgresql://user:password@localhost/pivoreddit'

# MySQL (требует установки)
SQLALCHEMY_DATABASE_URI = 'mysql+pymysql://user:password@localhost/pivoreddit'
```

---

## 🛑 Если что-то не работает

### Проблема: "ModuleNotFoundError: No module named 'flask'"

```powershell
# Решение: переустановите зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

### Проблема: "Port 5000 is already in use"

```powershell
# Решение: используйте другой порт в run.py
# или остановите другое приложение на порту 5000
```

### Проблема: "database is locked"

```powershell
# Решение: прочитаем БД
del app.db
python init_db.py
python run.py
```

### Проблема: Пусто при открытии

```powershell
# Решение: создайте тестовые данные
python init_db.py
```

---

## 📊 Файловая структура

```text
пивоо/
├── app.py               ← Основное приложение
├── models.py            ← База данных (моделии)
├── forms.py             ← Формы (валидация)
├── config.py            ← Конфигурация
├── run.py               ← Запуск приложения
├── init_db.py           ← Создание тестовых данных
├── requirements.txt     ← Зависимости Python
├── app.db               ← База данных (создается автоматически)
├── templates/           ← HTML шаблоны
│   ├── base.html
│   ├── auth/ (login, register)
│   ├── posts/ (create, view, edit)
│   ├── user/ (profile, settings)
│   ├── subreddits/ (list, create, edit)
│   ├── messages/ (inbox, view, send)
│   ├── admin/ (panel, users, reports)
│   ├── users/ (list of users)
│   ├── errors/ (404, 403, 500)
│   └── search.html
├── static/
│   ├── css/style.css    ← Стили (CSS)
│   └── js/main.js       ← Скрипты (JavaScript)
└── README.md            ← Документация
```

---

## 🎓 Полезные команды

```powershell
# Внутри выполнения приложения (в браузере):

# Просмотр консоли (F12) - DevTools
# Проверка логов в PowerShell

# Остановка приложения: Ctrl+C
# Перезагрузка: Ctrl+R в браузере

# Сброс пароля: закройте и откройте приложение заново
# Очистка файлов: python init_db.py
```

---

## 🔐 Безопасность

⚠️ **Важно для продакшена:**

1. Измените SECRET_KEY в config.py
2. Установите HTTPS (SSL сертификат)
3. Используйте правильные переменные окружения
4. Не херанвите пароли в коде
5. Используйте переменные FLASK_ENV='production'

---

## 🚀 После запуска

### Система готова с функциями

✅ Авторизация пользователей
✅ Создание и редактирование постов
✅ Система комментариев
✅ Лайски/дизлайки
✅ Сообщества (сабреддиты)
✅ Профили пользователей
✅ Личные сообщения
✅ Поиск
✅ Админ-панель
✅ Темная/светлая тема
✅ Мобильная версия
✅ И еще 78+ функций!

---

## 💡 Что дальше

### Попробуйте

1. Создать свой аккаунт
2. Создать сообщество
3. Написать пост
4. Оставить комментарий
5. Упвоутить/даунвоутить пост
6. Написать личное сообщение
7. Зайти как админ (admin/admin123)
8. Просмотреть админ-панель

### Для разработки

1. Изучите models.py - структура БД
2. Посмотрите app.py - маршруты
3. Отредактируйте templates для своего дизайна
4. Добавьте свои функции

---

## 📞 Помощь

Если что-то не работает:

1. Проверьте терминал на ошибки
2. Откройте DevTools (F12) в браузере
3. Перезагрузитесь: Ctrl+C и python run.py
4. Очистите БД: python init_db.py

---

Спасибо за использование PivooReddit

